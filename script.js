// Optional JavaScript code for contact form submission handling
const form = document.getElementById('contact-form');

form.addEventListener('submit', (e) => {
    e.preventDefault();
    // Code for form submission handling goes here
    // You can use AJAX or any other technique to send the form data to the server
});
